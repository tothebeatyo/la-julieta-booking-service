Build a production-ready Node.js Messenger chatbot for my aesthetic clinic "La Julieta Beauty Parañaque".

REQUIREMENTS:

1. Use Node.js with Express.

2. Create a webhook server for Facebook Messenger.

3. Endpoint:

   * GET /webhook (for verification)
   * POST /webhook (to receive messages)

4. Use environment variables:

   * PAGE_ACCESS_TOKEN
   * VERIFY_TOKEN
   * PORT

5. Create clean project structure:
   /routes
   /services
   /flows
   /utils

6. Implement Messenger send API using fetch.

7. Build a human-like Taglish chatbot flow (very important):

   * Friendly, conversational, not robotic
   * Tone: warm, polite, Taglish (Filipino + English)
   * Add typing delay (1–3 seconds)
   * Use varied responses (not repetitive)

8. Conversation flow:

   * Welcome message with variation
   * Ask user intent (book, services, promos, staff)
   * Booking flow:
     service → date → time → name → mobile → confirmation
   * Confirm details before booking
   * Allow "Edit" or "Confirm"
   * Always allow "Talk to Staff"

9. Add simple intent detection:

   * If user types "facial", "laser", etc → detect service
   * If user says "tomorrow", "today" → accept as date input

10. Add in-memory state management using Map (per user PSID).

11. Create placeholder booking service:

* File: services/anyplusService.js
* Function: createReservation(payload)
* For now return mock success

12. Add error handling:

* If booking fails → fallback message
* If input unclear → ask again naturally

13. Add logging for debugging incoming webhook events.

14. Make responses feel human:

* Use phrases like:
  "Sure po 💖"
  "Noted po ✨"
  "Let me check lang ha"
  "Got it po"
* Avoid robotic phrases like "Invalid input" or "Processing request"

15. Include quick replies where appropriate.

16. Include instructions at the end:

* how to run server
* how to set env variables
* how to connect to Facebook webhook

IMPORTANT:

* Code must be clean, modular, and ready to deploy on Replit
* No TypeScript, use plain JavaScript
* Use node-fetch
* Keep everything simple but production-ready

OUTPUT:
Generate all files with complete code.
