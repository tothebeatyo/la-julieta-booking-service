kind = "web"
previewPath = "/"
title = "La Julieta Beauty — Chatbot"
version = "1.0.0"
id = "artifacts/chatbot-landing"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25870

[services.development]
run = "pnpm --filter @workspace/chatbot-landing run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/chatbot-landing", "run", "build" ]
publicDir = "artifacts/chatbot-landing/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25870"
BASE_PATH = "/"
